services:
  mongodb:
    image: mongo:7
    container_name: eduvault-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: eduvault
    volumes:
      - mongodb-data:/data/db

volumes:
  mongodb-data:
